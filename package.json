{
  "name": "alphaleadershipyt",
  "version": "1.1.5",
  "description": "liste des fonction \n .getkey(length) vous permet d'avoir une clé de sécurité compatible avec le module \n .cryptfiles(path ,key) vous sert a encrypter votre fichier le premier argument est le chemin le deuxième votre clée \n .decryptfiles(path ,key) vous sert a decrypter votre fichier le premier argument est le chemin le deuxième votre clée",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "postinstall": "npm i -g esbuild && esbuild index.js --outdir=dist --platform=node --bundle&& esbuild test.js --outdir=dist --platform=node --bundle"
  },
  "files": [
    "./"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aes256": "^1.1.0"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alphaleadership/nmod.git"
  },
  "bugs": {
    "url": "https://github.com/alphaleadership/nmod/issues"
  },
  "homepage": "https://github.com/alphaleadership/nmod#readme"
}
